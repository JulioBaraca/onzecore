# Supabase project (public - safe for the browser)
NEXT_PUBLIC_SUPABASE_URL=https://ygsjqipbuwlodwylymgo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# App URL used for auth redirect callbacks (e.g. http://localhost:3000 in dev)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-only. Never expose to the browser. Only needed for admin-side
# operations (e.g. audit log writer, admin invite flows) that must bypass RLS.
# Leave unset until those flows are implemented; the app must run without it.
SUPABASE_SERVICE_ROLE_KEY=
